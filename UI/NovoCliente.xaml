<Window x:Class="UI.NovoCliente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Novo Cliente"
        Height="520" Width="720"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Background>
        <ImageBrush ImageSource="/Copy of DreamLand.png"
                    Stretch="UniformToFill"/>
    </Window.Background>
    
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- ================= LINHA 1 ================= -->

        <StackPanel Grid.Row="0" Margin="0,0,10,10">
            <Label Content="Nome"/>
            <TextBox Name="boxNome"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,10">
            <Label Content="CPF"/>
            <TextBox Name="boxCPF"/>
        </StackPanel>

        <!-- ================= LINHA 2 ================= -->

        <StackPanel Grid.Row="1" Margin="0,0,10,10">
            <Label Content="Email"/>
            <TextBox Name="boxEmail"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,10">
            <Label Content="Telefone"/>
            <TextBox Name="boxTelefone"/>
        </StackPanel>

        <!-- ================= LINHA 3 ================= -->

        <StackPanel Grid.Row="2" Margin="0,0,10,10">
            <Label Content="Saldo"/>
            <TextBox Name="boxSaldo" />
        </StackPanel>

        <!-- ================= BOTÕES ================= -->

        <Grid Grid.Row="4" Grid.ColumnSpan="2" Margin="0,20,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <CheckBox Name="boxAtivo"
                      Content="Cliente Ativo"
                      VerticalAlignment="Center"
                      IsChecked="True"/>

            <StackPanel Orientation="Horizontal" Grid.Column="1">

                <Button Content="Cancelar"
                        Width="120"
                        Margin="0,0,10,0"
                        IsCancel="True"/>

                <Button Content="Confirmar"
                        Width="120"
                        Click="btnConfirmarCliente"/>
            </StackPanel>

        </Grid>

    </Grid>
</Window>